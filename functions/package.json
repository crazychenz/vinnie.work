{
  "name": "firebase",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "yarn rimraf functions",
    "build": "yarn babel app --out-dir functions --copy-files --ignore app/node_modules",
    "postbuild": "cd functions && yarn"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^7.14.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-react": "^7.21.5",
    "firebase-tools": "^8.16.2",
    "rimraf": "^3.0.2"
  }
}
